import { Electrolize, Inter, Russo_One } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const titleFont = Russo_One({ weight: "400", subsets: ["latin"] });
export const normalFont = Electrolize({ weight: "400", subsets: ["latin"] });
